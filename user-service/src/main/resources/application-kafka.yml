spring:
  config:
    activate:
      on-profile: local
  kafka:
    bootstrap-servers: "localhost:9092"

---
spring:
  config:
    activate:
      on-profile: prod
  kafka:
    bootstrap-servers: "localhost:9092"