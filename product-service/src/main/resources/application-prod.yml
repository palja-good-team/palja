server:
  port: 8084

spring:
  application:
    name: product-service

  config:
    activate:
      on-profile: prod

  profiles:
    include:
      - datasource
      - zipkin
      - eureka
      - redis
      - swagger
      - monitor
      - kafka