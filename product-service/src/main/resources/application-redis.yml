spring:
  config:
    activate:
      on-profile: local
  data:
    redis:
      host: "localhost"
      port: 6379
redis-key:
  map0: "productStock0"
  map1: "productStock1"
  time-suffix: "Time"

---
spring:
  config:
    activate:
      on-profile: prod
  data:
    redis:
      host: ${REDIS_HOST}
      port: 6379
      password: ${REDIS_PASSWORD}
redis-key:
  map0: "productStock0"
  map1: "productStock1"
  time-suffix: "Time"

