server:
  port: 8080

spring:
  application:
    name: gateway-service

  config:
    activate:
      on-profile: prod

  profiles:
    include:
      - eureka
      - route
      - security
      - redis
      - swagger
      - monitor