server:
  port: 8081

spring:
  application:
    name: payment-service

  profiles:
    include:
      - datasource
      - zipkin
      - eureka
      - swagger

toss:
  base-url: https://api.tosspayments.com
  secret-key: ${TOSS_SECRET_KEY}