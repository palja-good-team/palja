server:
  port: 8086

spring:
  application:
    name: coupon-service

  kafka:
    bootstrap-servers: "localhost:9092"
    consumer:
      group-id: coupon-service

  profiles:
    include:
      - datasource
      - zipkin
      - redis
      - eureka
      - swagger
      - monitor
      - kafka