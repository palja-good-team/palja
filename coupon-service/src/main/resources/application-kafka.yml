spring:
  config:
    activate:
      on-profile: local
kafka:
  bootstrap-servers: "localhost:9092"
  consumer:
    group-id: coupon-service

---
spring:
  config:
    activate:
      on-profile: prod
kafka:
  bootstrap-servers: "localhost:9092"
  consumer:
    group-id: coupon-service